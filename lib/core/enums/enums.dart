enum LanguageEnum { AR, KR_SORANI, KR_BADANI }

enum RemoteDataStatus { ideal, loading, subloading, loaded, error, cache }

enum LeaveType {
  FULL,
  HALF,
}

enum LeaveDesition { APPROVE, REJECT, PENDING }

enum AppType {
  MANAGER,
  USER,
}

enum DataStatus {
  ideal,
  loading,
  subloading,
  subSuccess,
  loaded,
  error,
}
